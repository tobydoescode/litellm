[project]
name = "litellm-build"
version = "0"
description = "Multi-arch Docker build of LiteLLM for the home-lab k3s cluster"
requires-python = ">=3.14,<3.15"
dependencies = [
    "langfuse>=2.60.0,<3",
    "litellm[proxy]==1.92.1",
    "prisma==0.15.0",
]

[tool.uv]
package = false
