[project]
name = "litellm-build"
version = "0"
description = "Multi-arch Docker build of LiteLLM for the home-lab k3s cluster"
requires-python = ">=3.13,<3.14"
dependencies = [
    "langfuse>=4.7.1,<5",
    "litellm[proxy]==1.89.5",
    "prisma==0.15.0",
]

[tool.uv]
package = false
